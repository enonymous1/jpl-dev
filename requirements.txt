# Core Flask dependencies
# Requires Python 3.9+ (uses PEP 585 built-in generic types)
Flask==3.1.1
Frozen-Flask==1.0.2

# Standard library modules used (no install needed, but documenting)
# json - built-in
